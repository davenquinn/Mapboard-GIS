{
  "name": "mapboard-gis-docs",
  "version": "1.0.0",
  "description": "Documentation for the Mapboard GIS project",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel --dist-dir dist --public-url ./ ./src/index.js",
    "build": "parcel build --dist-dir dist ./src/index.js",
    "deploy": "scripts/deploy"
  },
  "author": "Daven Quinn",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "parcel-transformer-pandoc": "^1.0.0",
    "posthtml-include": "^1.5.0",
    "posthtml-md": "^1.1.0",
    "stylus": "^0.54.8"
  },
  "dependencies": {
    "@macrostrat/hyper": "^1.2.11",
    "posthtml-markdown": "^0.1.0",
    "require-from-string": "^2.0.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
